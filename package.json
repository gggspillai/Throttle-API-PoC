{
  "name": "throttle-api-poc",
  "version": "1.0.0",
  "description": "Implement a basic API throttling system from scratch, which serves HTTP(S) requests which  Normally serves the requested resource  throttles requests from a REFERRER when the number of requests exceeds X/sec, where X is configurable",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gggspillai/Throttle-API-PoC.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gggspillai/Throttle-API-PoC/issues"
  },
  "homepage": "https://github.com/gggspillai/Throttle-API-PoC#readme",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22"
  }
}
